/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# rspec failure tracking
.rspec_status

# Gemfile and Gemfile.original might be added multiple times, the ones in the repo will not be updated often, just exclude them
Gemfile
Gemfile.original